import appLocalData from "@formatjs/intl-pluralrules/locale-data/en"
import enMessages from "../locals/en-us"

const EnLang = {
    messages: {
        ...enMessages
    },
    locale: "en-us",
    data: appLocalData
}

export default EnLang